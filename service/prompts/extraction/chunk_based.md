【提取目标】
根据用户的提取需求，从提供的内容中提取所需信息。

【用户提取需求】
{{extraction_query}}

【项目背景（可选）】
{{project_desc}}

【内容】
{{selected_chunks_content}}

【提取要求】
1. **精准提取**：
   - 严格按照用户需求提取信息
   - 保持原文准确性，不要改写或总结（除非用户要求）
   - 如果需要提取原文，保持原有的格式和措辞

2. **完整性**：
   - 提取所有相关信息，不要遗漏
   - 如果信息分散在多处，应该整合

【输出格式】
- 仅输出提取结果，不要添加其他内容。
- 若无提取结果，请输出"无"。
- 输出结果必须为**纯文本**，不包含任何格式标记：
  - 不得包含 Markdown 格式（如 **加粗**、# 标题 ,列表格式）
  - 不得包含 HTML 标签（如 <p>、<table>、<strong>、<tr>、<td> 等）
  - 如遇到 HTML 表格，将其转换为纯文本描述，表格内容按行展示，列之间用制表位分隔

【注意事项】
- 保持原文的准确性和完整性
- 如果内容不足以回答用户需求，明确说明
- 输出应该直接可用，无需额外处理
